Try with a fresh install to ensure dependencies are correct

Database:
    design/database.txt
    Get database matching app needs
        weights
Endpoints:
    Create endpoints that match what the app needs as well

Endpoint for getting activities for specific user

Make adding users/activities/weights less error prone
    Not being able to add activity to non-existant user
